<resources>
    <!-- Base application theme. -->
    <style name="MyTheme" parent="Theme.AppCompat.DayNight">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/dark</item>
        <item name="colorPrimaryVariant">@color/gray</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/blue</item>
        <item name="colorSecondaryVariant">@color/light_blue</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/gray</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/dark</item>
        <item name="actionBarStyle">@style/MyTheme.ActionBar</item>
    </style>

    <style name="MyTheme.ActionBar" parent="Widget.AppCompat.ActionBar">
        <item name="background">@color/gray</item>
    </style>

    <style name="MyTheme.TabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/light_blue</item>
        <item name="tabSelectedTextColor">@color/light_blue</item>
        <item name="tabTextColor">@color/white</item>
    </style>

    <style name="MyTheme.TextAppearance.Medium" parent="TextAppearance.AppCompat.Medium">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="MyTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="MyTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark" />

    <style name="MyTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Dark" />
</resources>